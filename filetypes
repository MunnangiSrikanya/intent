{selectedFileType === 'Additional Medicare Intent' ? (
            <AdditionalMedicare />
          ) : selectedFileType === 'CMS BID' ? (
            <CmsBid />
          ) : selectedFileType === 'Clinical Supplements' ? (
            <ClinicalSupplement />
          ) : (
            ''
          )}
